package algorithm.asymptotic_notation;

public class Master_Theorem {

	// T(n) = aT(n/b) + THETA(n^klog^pn) where a >=1, b >1, k >=0 and p is a real number
	
	public static String serve(String input) {
		String output = "";
		
		return output;
	}
}
